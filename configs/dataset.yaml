# PyroPredict YOLO dataset configuration.
# This file can be generated by src/data/split.py or edited manually.

# Absolute or relative path to the split dataset root.
path: ../data/processed/smoke_yolo

train: images/train
val: images/val
test: images/test

# D-Fire class names.
names:
  0: fire
  1: smoke
